diff -ur a/packages/app-desktop/ElectronAppWrapper.ts b/packages/app-desktop/ElectronAppWrapper.ts
--- a/packages/app-desktop/ElectronAppWrapper.ts	2025-09-01 23:50:43.000000000 +1200
+++ b/packages/app-desktop/ElectronAppWrapper.ts	2025-09-09 21:23:10.613538159 +1200
@@ -612,7 +612,11 @@
 					console.warn('The window object was not available during the click event from tray icon');
 					return;
 				}
-				this.mainWindow().show();
+				if(!this.mainWindow().isVisible()){
+					this.mainWindow().show();
+				} else {
+					this.mainWindow().hide();
+				}
 			});
 		} catch (error) {
 			console.error('Cannot create tray', error);
